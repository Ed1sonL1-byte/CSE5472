#!/bin/sh
set -eu
seedbridge_root=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
cd "$seedbridge_root"
# Explicit source path also works when macOS marks editable .pth files hidden.
export PYTHONPATH="$seedbridge_root/src"
exec uv run --frozen python -m seedbridge.cli "$@"
